# Outline MCP Server — Environment Variables
#
# Two usage modes:
#
# 1. Plain values — copy to .env and fill in:
#      OUTLINE_API_KEY=ol_api_abc123...
#
# 2. 1Password CLI — copy to .env.op and use `op run`:
#      OUTLINE_API_KEY=op://Vault/Item/api-key
#
# See README.md for full setup instructions.

# Required for stdio mode, optional for HTTP/SSE (can use headers instead)
OUTLINE_API_KEY=your_outline_api_key_here

# Optional — defaults to https://app.getoutline.com/api
# OUTLINE_API_URL=https://app.getoutline.com/api

# Optional — HTTP/SSE transport settings
# OUTLINE_MCP_PORT=6060
# OUTLINE_MCP_HOST=127.0.0.1
