# ============================================================================
#  OUTLINE MCP SERVER CONFIGURATION
# ============================================================================

# OUTLINE_API_KEY: Your Outline API Key
# - REQUIRED for STDIO mode (validated on startup)
# - OPTIONAL for HTTP/SSE modes (can be provided via request headers instead)
# - When using HTTP/SSE without this env var, you must provide x-outline-api-key header
OUTLINE_API_KEY=your_outline_api_key_here

# OUTLINE_API_URL: Outline API base URL
# - OPTIONAL for all modes (defaults to https://app.getoutline.com/api)
# - Can be overridden via x-outline-api-url header in HTTP/SSE modes
# - Uncomment and modify if using a self-hosted instance or custom domain
# OUTLINE_API_URL=https://app.getoutline.com/api

# Optional: MCP Server Configuration
# Host/IP to bind the server to (defaults to 127.0.0.1)
# Use 0.0.0.0 to bind to all network interfaces for multi-client access
# OUTLINE_MCP_HOST=127.0.0.1

# Optional: MCP Server Port (defaults to 6060)
# OUTLINE_MCP_PORT=6060

# To get your Outline API key:
# 1. Log in to your Outline account
# 2. Go to Settings > API Tokens
# 3. Create a new token and copy it
# 4. Create a .env file and paste your key here

# Multi-Instance Support:
# The server supports multiple Outline instances via per-request headers:
# - x-outline-api-key: API key for the specific instance
# - x-outline-api-url: API URL for the specific instance
#
# These headers override the environment variables for individual requests,
# allowing you to interact with multiple Outline instances from the same server.
#
# Example headers:
#   x-outline-api-key: your_api_key_for_instance_1
#   x-outline-api-url: https://outline1.example.com  # /api is automatically added if missing
#
# Note: The /api suffix is automatically appended if not present in the URL.
# If not provided in headers, the server falls back to the environment variables above.
